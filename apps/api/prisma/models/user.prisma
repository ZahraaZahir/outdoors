enum Role {
  TRAVELER
  ADMIN
}

model User {
  id          Int       @id @default(autoincrement())
  email       String    @unique
  password    String
  name        String
  role        Role      @default(TRAVELER)
  phoneNumber String
  bookings    Booking[]
  createdAt   DateTime  @default(now())
}
